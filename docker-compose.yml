services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
      - ./gdrive-credentials.json:/app/gdrive-credentials.json:ro
    env_file:
      - .env
